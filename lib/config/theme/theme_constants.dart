part of 'theme_app.dart';

// Spacing
const spacingXS = 4.0;
const spacingS = 8.0;
const spacingM = 12.0;
const spacingL = 16.0;
const spacingXL = 24.0;

// Radius
const borderRadiusS = 4.0;

// Dialog sizes
const dialogMaxWidth = 420.0;
const dialogInsetHorizontal = 16.0;
const dialogInsetVertical = 24.0;

// Animation durations
const animationDurationFast = Duration(milliseconds: 150);
const animationDurationNormal = Duration(milliseconds: 300);
const animationDurationSlow = Duration(milliseconds: 500);
